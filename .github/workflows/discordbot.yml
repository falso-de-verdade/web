name: discord bot

on:
  push:
    branches: [ feat-discord-bot ]

jobs:
  notificate:
    runs-on: ubuntu-latest
    steps:
      - env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
